name: square
version: 0.1.0

authors:
  - Jaired Jawed <me@jairedjawed.com>

description: |
  Crystal client SDK for the Square API

dependencies:
  dotenv:
    github: gdotdesign/cr-dotenv

#   pg:
#     github: will/crystal-pg
#     version: "~> 0.5"

# development_dependencies:
#   webmock:
#     github: manastech/webmock.cr

license: MIT
